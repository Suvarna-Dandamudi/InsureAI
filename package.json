{
  "name": "insureai",
  "version": "1.0.0",
  "description": "AI Insurance SaaS Platform",
  "main": "backend/server.js",
  "scripts": {
    "server": "node backend/server.js",
    "dev": "concurrently \"npm run server\" \"cd frontend && npm start\"",
    "install-deps": "npm install && cd frontend && npm install",
    "build": "cd frontend && npm run build"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "helmet": "^7.0.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^7.8.9",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.1"
  }
}
